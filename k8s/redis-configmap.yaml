apiVersion: v1
kind: ConfigMap
metadata:
  name: keda-redis-config
  namespace: default
data:
  redis-host: "redis-headless.default.svc.cluster.local"
  redis-port: "6379"
  stream-name: "vllm_task_stream"
  group-name: "vllm_inference_group"